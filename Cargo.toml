[package]
name    = "cnc_uploader"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "cnc_uploader"
path = "src/main.rs"

[dependencies]
# Cross-platform serial port library – works on Windows, Linux, macOS.
# Wraps the Windows CreateFile / SetCommState / WriteFile API internally.
serialport = "4"
